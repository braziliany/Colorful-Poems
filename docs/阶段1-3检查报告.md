# 彩云诗词桌面天气｜阶段 1～3 检查报告

> 检查日期：2026-08-01  
> 检查范围：安全、v1.1.1 稳定性、视觉细节  
> 执行方式：只读审计与自动化测试；未进行开源整理，未修改组件源码，未提交或推送

## 一、结论

当前版本的核心功能、Midnight Theme、双栏结构、Token 占位符和安装器完整性均通过检查，但尚不建议直接进入稳定版开源整理。

需要确认后处理的项目：

1. `.gitignore` 缺少路线要求的 `*.key`、`*.token`、`config.private.js`、`.env`。
2. 两张已删除截图仍存在于 Git 历史，内容包含杭州天气预警及具体日程信息；当前分支已不跟踪图片，本地截图也已被忽略。
3. 控制台会输出城市、区县和电池百分比。日志不会上传，也未输出 Token，但按严格隐私标准应默认关闭这两类状态日志。
4. 预警文字与分割线已使用官方等级色；当前 UI 没有独立预警图标，因此“预警图标跟随等级色”没有实际应用对象。

## 二、阶段 1：代码与仓库安全检查

### 2.1 Token 安全

状态：通过。

- 当前 `src/widget.js` 仅包含 `YOUR_CAIYUN_API_KEY` 占位符。
- 检查全部 4 个 Git 提交，彩云 Key 均为占位符，未发现非占位值。
- 安装器使用 Scriptable `addSecureTextField()` 接收 Token。
- Token 只注入本机 iCloud 组件副本，安装器没有输出 Token 的日志。
- 今日诗词 Token 由 Scriptable 本地运行时获取并保存，没有进入仓库。

### 2.2 个人配置与私密路径

状态：通过。

- 默认经纬度、城市、区县和街道均为 `undefined`。
- `lockLocation` 默认关闭。
- 跟踪文件中未发现 `C:\Users\...`、`F:\...` 或其他本机绝对路径。
- 未发现私钥、密码或真实 API 凭据。

### 2.3 截图与 Git 历史

状态：当前分支通过，历史仍有风险。

- 当前 Git 树没有跟踪 PNG、JPG、HEIC 或 GIF。
- `.gitignore` 已忽略整个 `screenshots/` 目录。
- 本地 `before.jpg` 和 `after.png` 不会被重新提交。
- Git 历史仍保留两个截图 Blob：`screenshots/before.jpg`、`screenshots/after.png`。
- 截图可见杭州高温预警，其中一张还包含“牛奶咖啡演唱会 18:00→19:00”日程。

风险说明：公开仓库使用者仍可从旧提交恢复这些图片。若按“无个人截图”严格验收，需要重写 Git 历史并强制推送；这是破坏性仓库操作，必须单独确认。

### 2.4 日志检查

状态：部分通过。

- 未发现 Token、请求头、完整接口响应或经纬度日志。
- 天气成功后会记录“城市·区县”。
- 电池读取后会记录电池百分比。
- 其他日志主要是接口、权限、图片与 SF Symbol 错误信息。

建议：保留错误日志；移除城市/区县和电池状态日志，或增加默认关闭的调试开关。

### 2.5 `.gitignore`

状态：不符合路线要求。

当前仅包含：

```gitignore
screenshots/
```

待补充：

```gitignore
*.key
*.token
config.private.js
.env
```

## 三、阶段 2：v1.1.1 稳定版本检查

### 3.1 自动化与仓库状态

状态：通过。

- 工作区检查前为干净状态。
- `installer.test.js` 通过。
- `widget-battery.test.js` 通过。
- `release-integrity.test.js` 通过。
- `git diff --check` 通过。
- GitHub `main` 与本地提交 `1f57b9e` 一致。
- Manifest、组件源码、安装器和 Changelog 四个远端资源均返回 HTTP 200。

### 3.2 功能检查

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 天气 | 条件通过 | 字段与网络失败兜底完整；公开源码无 Token，无法在电脑端执行真实天气请求；此前真机已验证有效 Token 场景。 |
| 农历 | 通过 | 优先读取 `lunar_date`，兼容旧 `lunar` 字段，失败显示兜底。 |
| 日程 | 通过 | 返回数组、日期对象、全天事件及单条异常均有保护。 |
| 电池 | 通过 | 100%、55%、20%、低电量和充电状态自动化测试通过。 |
| 预警颜色 | 通过 | 红、橙、黄、蓝映射值正确，预警文字与现有分割线使用映射色。 |
| 透明背景 | 条件通过 | 相册取消、无效图片、未知截图尺寸和裁剪失败均有兜底；仍需真机走一次完整生成流程。 |
| 安装器 | 通过 | 版本比较、隐藏输入、Token 注入、更新保留、资源校验和发行完整性测试通过；远端资源可访问。 |

### 3.3 Scriptable 与 UI 不变量

状态：通过。

- 使用 `ListWidget` 与 Scriptable Stack，保持水平双栏。
- 右栏宽度仍为 110pt。
- 保留深蓝紫 Midnight Theme 和黄色 Accent。
- 保留 `Good afternoon~` Unicode 特殊字体效果。
- 所有底层 `addImage()` 输入统一经过安全图片处理。
- `SFSymbol.named()` 统一经过异常保护和备用图标。
- 未发现实际运行的浏览器或 Node.js API。
- 本轮没有增加功能或改变布局结构。

## 四、阶段 3：视觉细节检查

### 4.1 电池层级

状态：通过。

- 电池位于右下角，与更新时间属于同一系统状态层级。
- 图标为 `12×7`，百分比为 8pt。
- 正常电量：`#AFA6CC`。
- 低电量：`#FF453A`。
- 充电状态：`#30D158`。
- 电池不再占用左侧农历行空间。

### 4.2 预警颜色

状态：部分通过。

- 红色：`#FF3B30`。
- 橙色：`#FF9500`。
- 黄色：`#FFD60A`。
- 蓝色：`#0A84FF`。
- 预警文字使用等级色。
- 日程上方的现有分割线使用等级色。
- 当前布局没有独立预警图标；`megaphone.fill` 是日程图标并继续使用主题黄色。

待确认：

- 推荐方案：保持现有布局，将“预警图标”标记为不适用，不新增图标。
- 可选方案：在预警文字前增加小型图标并使用等级色，但这会改变现有 Stack 内容和文字可用宽度。

## 五、进入开源整理前的建议处理清单

建议按以下顺序执行，全部需要用户确认：

1. 补齐 `.gitignore` 的四类敏感文件规则。
2. 移除城市/区县及电池百分比的常规日志，保留错误日志。
3. 决定是否重写 Git 历史以彻底移除两张旧截图。
4. 确认预警图标采用“不适用”还是“新增小图标”方案。
5. 修复后重新运行测试并进行一次 Scriptable 真机回归。

在以上事项确认前，不执行阶段 4～7，不创建稳定版提交、Tag 或 GitHub Release。
